 import 'package:flutter/material.dart';


 class PrepayementPage extends StatelessWidget {
  const PrepayementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Background(
        child: const Center(
          child: Text("prepayementpage"),
        ),
      ),
    );
  }
 }
